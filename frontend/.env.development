# Konfigurasi dev — frontend mengarah ke backend lokal.
VITE_API_BASE_URL=http://localhost:4000/api
VITE_SOCKET_URL=http://localhost:4000

# Untuk kembali ke mode mock (tanpa backend), set true (mis. di .env.local):
# VITE_USE_MOCKS=true
